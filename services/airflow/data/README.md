# Airflow data folder

`example_dag_0_upload` reads the Superstore CSV from this folder inside the Airflow container:

```text
/opt/airflow/data/Superstore-Sample.csv
```

On your Mac this path is:

```text
services/airflow/data/Superstore-Sample.csv
```
